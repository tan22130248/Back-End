spring:
  application:
    name: Back-End
  config:
    import: optional:file:./.env
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
  mail:
    host: smtp.gmail.com
    port: 587
    username: 22130248@st.hcmuaf.edu.vn
    password: wnqe ckyg iutr ekxn
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  datasource:
    url: jdbc:mysql://localhost:3306/web_audio?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=utf8
    username: webaudio
    password: webaudio
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

jwt:
  secret: WebAudioVOX2026SecretKeyForJWTTokenGeneration
  expiration: 86400000

cloudflare:
  r2:
    endpoint: ${R2_ENDPOINT:https://ce93c7c795116afd02f973d455765f75.r2.cloudflarestorage.com}
    bucket: ${R2_BUCKET:web-audio}
    access-key: ${R2_ACCESS_KEY:}
    secret-key: ${R2_SECRET_KEY:}
    public-domain: ${R2_PUBLIC_DOMAIN:https://pub-902e8561cd8a4a2fab20f1c5ecbd3054.r2.dev}

server:
  port: 8080
